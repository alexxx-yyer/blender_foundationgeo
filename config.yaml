# （YAML）
# ：
# -  null default
# - CLI file

input: /home/alex/projects/FoundationGeo/data/blender/blender-4.5-splash.blend
output: /home/alex/projects/FoundationGeo/scene

# Camera（）
camera: null

# Resolution（ null， Blender Scene）
width: null
height: null

# animationexport
export_animation: true
frame_start: null
frame_end: null
frame_step: 1

# 
skip_conversion: false
colormap: turbo

# Blender path（，null ）
# blender: null
blender: ~/blender-4.2.17-linux-x64/blender

# Render device（）
# device: CPU / GPU
# compute_type: CUDA / OPTIX / HIP / METAL / ONEAPI
device: GPU
compute_type: CUDA

# GPU （）
# gpu_ids: all          # GPU（default）
# gpu_ids: "0,1,2,3,4,5,6,7"  # GPU
gpu_ids: all
